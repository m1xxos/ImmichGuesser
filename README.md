# 📍 Immich Guesser

Веб-приложение в стиле GeoGuesser, использующее фотографии из вашего Immich сервера. Угадывайте локации по фотографиям с GPS-координатами!

## 🎮 Возможности

- 🔐 **Аутентификация пользователей** с JWT токенами
- 🎯 **Игровой процесс**: 5 раундов угадывания локаций
- 🗺️ **Интерактивная карта** на основе Leaflet.js + OpenStreetMap
- 📊 **Таблица лидеров** с лучшими результатами
- 🏆 **Система подсчета очков** на основе точности угадывания
- 📸 **Автоматическая загрузка фото** с GPS из Immich
- 🐳 **Docker развертывание** с docker-compose

## 🏗️ Архитектура

- **Backend**: FastAPI (Python 3.11)
- **Frontend**: Vanilla JS + Leaflet.js
- **База данных**: PostgreSQL
- **Кеш**: Redis
- **Контейнеризация**: Docker + Docker Compose

## 📋 Требования

- Docker и Docker Compose
- Доступ к Immich серверу с API ключом
- Фотографии с GPS-координатами в Immich

## 🚀 Установка и запуск

### 1. Клонирование репозитория

```bash
git clone <your-repo-url>
cd ImmichGuesser
```

### 2. Создание файла конфигурации

Скопируйте `.env.example` в `.env` и настройте переменные:

```bash
cp .env.example .env
```

### 3. Настройка `.env` файла

Откройте `.env` и заполните следующие параметры:

```env
# Immich Connection
IMMICH_API_URL=http://your-immich-instance:2283/api
IMMICH_API_KEY=your_immich_api_key_here

# App Security (сгенерируйте случайный ключ)
SECRET_KEY=your_jwt_secret_key_here

# Database (можно оставить по умолчанию)
POSTGRES_DB=immichguesser
POSTGRES_USER=immichuser
POSTGRES_PASSWORD=immichpass
DATABASE_URL=postgresql://immichuser:immichpass@db:5432/immichguesser

# Redis (можно оставить по умолчанию)
REDIS_URL=redis://redis:6379

# App Configuration
MIN_DISTANCE_KM=0.1
MAX_POINTS=5000
```

### 4. Получение Immich API ключа

1. Откройте ваш Immich в браузере
2. Перейдите в **Settings** → **Account Settings** → **API Keys**
3. Нажмите **New API Key**
4. Введите название (например, "ImmichGuesser")
5. **Выберите необходимые разрешения**:
   - ✅ **asset.read** - чтение фотографий (обязательно)
   - ✅ **asset.view** - просмотр фотографий (обязательно)
   - Другие разрешения не требуются
6. Скопируйте сгенерированный ключ и вставьте в `.env` файл как `IMMICH_API_KEY`

> ⚠️ **Важно**: API ключ должен иметь минимум права на чтение ассетов (`asset.read`). Без этого приложение не сможет загружать фотографии из Immich.

### 5. Генерация SECRET_KEY

Сгенерируйте случайный секретный ключ для JWT:

```bash
openssl rand -hex 32
```

Вставьте результат в `.env` файл как `SECRET_KEY`.

### 6. Запуск приложения

```bash
docker-compose up -d
```

Первый запуск может занять несколько минут для загрузки образов и установки зависимостей.

### 7. Проверка статуса

```bash
docker-compose ps
```

Все сервисы должны быть в статусе "Up".

### 8. Открытие приложения

- **Frontend**: http://localhost:3000
- **API документация**: http://localhost:8000/docs
- **API health check**: http://localhost:8000/api/health

## 🎮 Как играть

1. **Регистрация**: Создайте аккаунт на главной странице
2. **Вход**: Войдите с вашими учетными данными
3. **Старт игры**: Нажмите "Start New Game"
4. **Угадывание**:
   - Посмотрите на фотографию слева
   - Кликните на карте справа, чтобы указать предполагаемую локацию
   - Нажмите "Submit Guess"
5. **Результаты**: Смотрите расстояние и очки за каждый раунд
6. **Завершение**: После 5 раундов увидите финальный счет
7. **Таблица лидеров**: Сравните свои результаты с другими игроками

## 📊 Система подсчета очков

- **< 1 км**: ~4750 очков (95% от максимума)
- **1-10 км**: 2500-4750 очков (экспоненциальное убывание)
- **10-100 км**: 500-2500 очков (линейное убывание)
- **100-1000 км**: 50-500 очков
- **> 1000 км**: 10-50 очков

Максимум: **5000 очков** за идеальное угадывание.

## 🛠️ Разработка

### Структура проекта

```
ImmichGuesser/
├── backend/
│   ├── app/
│   │   ├── routers/          # API endpoints
│   │   ├── services/         # Бизнес-логика
│   │   ├── models/           # Pydantic модели
│   │   ├── database/         # SQLAlchemy модели
│   │   ├── config.py         # Конфигурация
│   │   ├── dependencies.py   # JWT аутентификация
│   │   └── main.py           # FastAPI приложение
│   ├── Dockerfile
│   └── requirements.txt
├── frontend/
│   ├── static/
│   │   ├── css/
│   │   │   └── style.css
│   │   └── js/
│   │       └── game.js
│   ├── index.html
│   ├── nginx.conf
│   └── Dockerfile
├── docker-compose.yml
├── .env.example
└── README.md
```

### Запуск в режиме разработки

Backend автоматически перезагружается при изменении кода (uvicorn --reload).

```bash
# Просмотр логов
docker-compose logs -f backend

# Перезапуск сервисов
docker-compose restart backend
docker-compose restart frontend
```

### API эндпоинты

#### Аутентификация
- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход
- `GET /api/auth/me` - Информация о пользователе

#### Игра
- `POST /api/game/start` - Начать новую игру
- `GET /api/game/current` - Текущая игра
- `GET /api/game/photo` - Получить фото для угадывания
- `POST /api/game/guess` - Отправить догадку
- `GET /api/game/rounds` - Список раундов
- `GET /api/game/leaderboard` - Таблица лидеров
- `DELETE /api/game/current` - Удалить текущую игру

## 🐛 Устранение неполадок

### Backend не запускается

```bash
# Проверьте логи
docker-compose logs backend

# Убедитесь, что .env файл настроен корректно
cat .env

# Пересоздайте контейнеры
docker-compose down
docker-compose up -d --build
```

### Нет фотографий с GPS

Убедитесь, что:
1. В Immich есть фотографии с GPS-координатами (EXIF данные)
2. API ключ имеет права на чтение фотографий
3. URL Immich правильный и доступен из Docker контейнера

```bash
# Проверьте подключение к Immich из контейнера
docker-compose exec backend curl http://your-immich-instance:2283/api/server-info
```

### База данных не инициализируется

```bash
# Пересоздайте базу данных
docker-compose down -v
docker-compose up -d
```

### Frontend не отображается

```bash
# Проверьте логи nginx
docker-compose logs frontend

# Убедитесь, что файлы скопированы
docker-compose exec frontend ls -la /usr/share/nginx/html
```

## 🔒 Безопасность

### Production

Для production окружения:

1. **Измените CORS настройки** в `backend/app/main.py`:
   ```python
   allow_origins=["https://yourdomain.com"]
   ```

2. **Используйте HTTPS** с reverse proxy (nginx, Caddy, Traefik)

3. **Генерируйте сильные пароли** для PostgreSQL

4. **Ограничьте доступ** к портам через firewall

5. **Используйте Docker secrets** вместо `.env` файла

6. **Обновляйте зависимости** регулярно

## 📝 Лицензия

MIT

## 🤝 Контрибуция

Pull requests приветствуются!

## 📧 Поддержка

Если у вас возникли проблемы, создайте issue в репозитории.

---

Сделано с ❤️ для любителей Immich и GeoGuesser

